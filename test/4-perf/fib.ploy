<fn fib {-n}
  <if
    (eq n 0) 0
    (eq n 1) 1
    <do
      <fn f {-n -a -b}
        <if (eq n 1) b
          (self (sub n 1) b (add a b))>>
      (f n 0 1)>>>

(outRL (fib 89)) # past this it looks like the c stack overflows.
