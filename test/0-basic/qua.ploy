<let x 0>
<let y `x>

(outRLL
x # 0
~x # `x
~,x # 0
~`,x # ``0
~~,x # `~,x
~~,,x # `~,0
~(CONS Arr-Expr x ,x `x `,x ~x ~,x ~,,x ~~,,x ~~,,,x)
##`(CONS Arr-Expr x 0 `x `0 ~x ~,x ~,0 ~~,,x ~~,,0)
~,y
(RUN ~,y)
)
