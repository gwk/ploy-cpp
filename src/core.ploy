# Copyright 2014 George King.
# Permission to use this file is granted in ploy/license.txt.


# define the 'macro' macro.
# this definition uses the raw vec syntax ^[...] to build syntax vectors directly.
# the definition is equivalent to:

##<macro macro (-sym -pars &body)
  (sym TRUE pars body {})>

(LET macro
  (QUO 
    ( macro
      TRUE
      (-sym -pars &body)
      (sym TRUE pars body CHAIN0)
      CHAIN0)))

<macro quo (expr) [QUO expr]>

